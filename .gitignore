drift-timeline.html
drift-viewer.html
node_modules
.drift-events.jsonl
.drift-state.json

.DS_Store
.aone_copilot/

# All eval corpora kept local only — GitHub holds code, not session data
eval/fixtures/
eval/fixtures-strong/
eval/fixtures-valid/
eval/fixtures_anonymized/
eval/quarantine/

# Auto-collected candidates (unreviewed, may contain sensitive data)
eval/candidates/

# Held-out blind eval — local real-session corpus + labels + reports (sensitive)
eval/heldout/

# Reports may contain local paths
eval/reports/
eval/timelines/

# Local agent / editor config (may contain absolute paths, personal data)
.claude/settings.local.json

# Publication drafts & personal working docs (may carry author identity / real cases)
docs/publish-drafts.md
docs/article-draft-why-agents-drift.md
docs/cases/
